## 修复DAO接口文件计划

### 问题分析
通过查看系统提醒，发现以下DAO接口文件为空或存在乱码问题：
1. `OrderDAO.java`
2. `ProductDAO.java`
3. `ShoppingCartDAO.java`
4. `UserDAO.java`

### 修复方案
根据对应的Impl实现类，重新创建这四个DAO接口文件，确保每个接口包含正确的方法签名。

### 实现步骤
1. 根据`OrderDAOImpl.java`，重新创建`OrderDAO.java`接口，包含7个方法
2. 根据`ProductDAOImpl.java`，重新创建`ProductDAO.java`接口，包含8个方法
3. 根据`ShoppingCartDAOImpl.java`，重新创建`ShoppingCartDAO.java`接口，包含6个方法
4. 根据`UserDAOImpl.java`，重新创建`UserDAO.java`接口，包含5个方法
5. 验证所有修复后的文件内容

### 预期效果
所有DAO接口文件将包含完整的方法签名，与实现类保持一致，为后续编译和打包做好准备。