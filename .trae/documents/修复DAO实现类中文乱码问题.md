## 修复DAO实现类中文乱码问题

### 问题分析
通过查看`src/com/shopping/dao/impl/`目录下的4个Java文件，发现以下中文乱码问题：

1. **OrderDAOImpl.java**：
   - 第97行：`获取订单�?` 应改为 `获取订单项`
   - 第135行：`获取订单�?` 应改为 `获取订单项`
   - 第172行：`获取订单�?` 应改为 `获取订单项`

2. **ShoppingCartDAOImpl.java**：
   - 第21行：`先检查购物车中是否已存在该商�?` 应改为 `先检查购物车中是否已存在该商品`
   - 第25行：`如果已存在，则更新数�?` 应改为 `如果已存在，则更新数量`

3. **ProductDAOImpl.java** 和 **UserDAOImpl.java**：无明显中文乱码问题

### 修复计划
1. 修复 `OrderDAOImpl.java` 中的3处中文乱码
2. 修复 `ShoppingCartDAOImpl.java` 中的2处中文乱码
3. 验证所有修复后的文件内容
4. 继续执行后续编译和打包任务

### 预期效果
所有DAO实现类中的中文注释将正确显示，不再出现乱码问题，为后续编译和打包做好准备。